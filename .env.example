# Veritabanı bağlantısı (SQLite - yerel dosya)
# Bu dosyayı .env olarak kopyalayın: cp .env.example .env
DATABASE_URL="file:./dev.db"

# JWT imzalama anahtarı (production'da mutlaka değiştirin)
JWT_SECRET="agroklinik-secret-key-2024"
